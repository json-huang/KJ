<UserControl x:Class="KJ.Modules.Monitoring.Views.MonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <Button Width="120" Margin="0,0,8,0" Content="Start" Command="{Binding StartCommand}" />
            <Button Width="120" Content="Stop" Command="{Binding StopCommand}" />
        </StackPanel>

        <Border Grid.Row="1" BorderThickness="1" BorderBrush="#FFCCCCCC" Padding="12">
            <StackPanel>
                <TextBlock FontWeight="SemiBold" Text="Heartbeat"/>
                <TextBlock Margin="0,6,0,0" FontSize="18" Text="{Binding Heartbeat}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>


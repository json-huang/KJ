<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="KJ.Modules.Monitoring.Views.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <ScrollViewer Padding="24">
        <Grid ColumnSpacing="16" RowSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.ColumnSpan="2" Spacing="6">
                <TextBlock FontSize="22" Text="Dashboard" />
                <TextBlock
                    Foreground="{StaticResource KjTextSecondaryBrush}"
                    Opacity="0.9"
                    Text="总览：设备在线率、告警态势、关键 Tag、最近事件。"
                    TextWrapping="WrapWholeWords" />
            </StackPanel>

            <!-- KPI row -->
            <Grid Grid.Row="1" Grid.ColumnSpan="2" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Background="{StaticResource KjPanel2Brush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="12" Padding="14,12">
                    <StackPanel Spacing="6">
                        <TextBlock Foreground="{StaticResource KjTextMutedBrush}" FontSize="12" Text="设备总数" />
                        <TextBlock FontSize="26" FontWeight="SemiBold" Text="42" />
                        <TextBlock Foreground="{StaticResource KjTextSecondaryBrush}" FontSize="12" Text="在线 39 · 离线 3" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="{StaticResource KjPanel2Brush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="12" Padding="14,12">
                    <StackPanel Spacing="6">
                        <TextBlock Foreground="{StaticResource KjTextMutedBrush}" FontSize="12" Text="告警（24h）" />
                        <TextBlock FontSize="26" FontWeight="SemiBold" Text="3" />
                        <TextBlock Foreground="{StaticResource KjTextSecondaryBrush}" FontSize="12" Text="高 1 · 中 2 · 低 0" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="{StaticResource KjPanel2Brush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="12" Padding="14,12">
                    <StackPanel Spacing="6">
                        <TextBlock Foreground="{StaticResource KjTextMutedBrush}" FontSize="12" Text="连接质量" />
                        <TextBlock FontSize="26" FontWeight="SemiBold" Text="99.2%" />
                        <TextBlock Foreground="{StaticResource KjTextSecondaryBrush}" FontSize="12" Text="平均延迟 18ms" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="3" Background="{StaticResource KjPanel2Brush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="12" Padding="14,12">
                    <StackPanel Spacing="6">
                        <TextBlock Foreground="{StaticResource KjTextMutedBrush}" FontSize="12" Text="Tag 采样" />
                        <TextBlock FontSize="26" FontWeight="SemiBold" Text="1,280" />
                        <TextBlock Foreground="{StaticResource KjTextSecondaryBrush}" FontSize="12" Text="近 1min 更新 1,112" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Left: Recent events -->
            <Border Grid.Row="2" Background="{StaticResource KjPanel2Brush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="12" Padding="14,12">
                <StackPanel Spacing="12">
                    <StackPanel Spacing="2">
                        <TextBlock FontWeight="SemiBold" Text="最近事件" />
                        <TextBlock Foreground="{StaticResource KjTextMutedBrush}" FontSize="12" Text="示例数据（后续接入事件/告警流）" />
                    </StackPanel>

                    <Grid RowSpacing="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Background="{StaticResource KjPanelBrush}" BorderBrush="{StaticResource KjStrokeSubtleBrush}" BorderThickness="1" CornerRadius="10" Padding="12,10">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border Background="{StaticResource KjAccentBrush}" CornerRadius="999" Padding="8,2">
                                    <TextBlock Foreground="{StaticResource KjBg}" FontSize="11" FontWeight="SemiBold" Text="ALARM" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="Mixer-02：温度超限（> 85℃）" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="2" Foreground="{StaticResource KjTextMutedBrush}" FontSize="12" Text="2m ago" />
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Background="{StaticResource KjPanelBrush}" BorderBrush="{StaticResource KjStrokeSubtleBrush}" BorderThickness="1" CornerRadius="10" Padding="12,10">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border Background="{StaticResource KjPanel2Brush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="999" Padding="8,2">
                                    <TextBlock Foreground="{StaticResource KjTextSecondaryBrush}" FontSize="11" FontWeight="SemiBold" Text="STATE" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="Edge-PLC-01：已恢复在线" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="2" Foreground="{StaticResource KjTextMutedBrush}" FontSize="12" Text="8m ago" />
                            </Grid>
                        </Border>

                        <Border Grid.Row="2" Background="{StaticResource KjPanelBrush}" BorderBrush="{StaticResource KjStrokeSubtleBrush}" BorderThickness="1" CornerRadius="10" Padding="12,10">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border Background="{StaticResource KjPanel2Brush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="999" Padding="8,2">
                                    <TextBlock Foreground="{StaticResource KjTextSecondaryBrush}" FontSize="11" FontWeight="SemiBold" Text="TAG" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="Tank-Level：液位 62%（稳定）" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="2" Foreground="{StaticResource KjTextMutedBrush}" FontSize="12" Text="13m ago" />
                            </Grid>
                        </Border>

                        <Border Grid.Row="3" Background="{StaticResource KjPanelBrush}" BorderBrush="{StaticResource KjStrokeSubtleBrush}" BorderThickness="1" CornerRadius="10" Padding="12,10">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border Background="{StaticResource KjPanel2Brush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="999" Padding="8,2">
                                    <TextBlock Foreground="{StaticResource KjTextSecondaryBrush}" FontSize="11" FontWeight="SemiBold" Text="JOB" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="配方 R-12 已下发至 3 台设备" TextTrimming="CharacterEllipsis" />
                                <TextBlock Grid.Column="2" Foreground="{StaticResource KjTextMutedBrush}" FontSize="12" Text="24m ago" />
                            </Grid>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Right: Quick actions + system status -->
            <StackPanel Grid.Row="2" Grid.Column="1" Spacing="16">
                <Border Background="{StaticResource KjPanel2Brush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="12" Padding="14,12">
                    <StackPanel Spacing="10">
                        <TextBlock FontWeight="SemiBold" Text="快速操作" />
                        <Button Style="{StaticResource PrimaryAction}" Content="刷新全量状态" />
                        <Button Style="{StaticResource Action}" Content="查看告警" />
                        <Button Style="{StaticResource Action}" Content="打开设备列表" />
                    </StackPanel>
                </Border>

                <Border Background="{StaticResource KjPanel2Brush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="12" Padding="14,12">
                    <StackPanel Spacing="10">
                        <TextBlock FontWeight="SemiBold" Text="系统状态" />
                        <Grid ColumnSpacing="10" RowSpacing="8">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Foreground="{StaticResource KjTextSecondaryBrush}" Text="数据库" />
                            <Border Grid.Column="1" Background="{StaticResource KjPanelBrush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="999" Padding="8,2">
                                <TextBlock FontSize="11" Foreground="{StaticResource KjTextSecondaryBrush}" Text="OK" />
                            </Border>

                            <TextBlock Grid.Row="1" Foreground="{StaticResource KjTextSecondaryBrush}" Text="消息总线" />
                            <Border Grid.Row="1" Grid.Column="1" Background="{StaticResource KjPanelBrush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="999" Padding="8,2">
                                <TextBlock FontSize="11" Foreground="{StaticResource KjTextSecondaryBrush}" Text="OK" />
                            </Border>

                            <TextBlock Grid.Row="2" Foreground="{StaticResource KjTextSecondaryBrush}" Text="实时采集" />
                            <Border Grid.Row="2" Grid.Column="1" Background="{StaticResource KjPanelBrush}" BorderBrush="{StaticResource KjStrokeBrush}" BorderThickness="1" CornerRadius="999" Padding="8,2">
                                <TextBlock FontSize="11" Foreground="{StaticResource KjTextSecondaryBrush}" Text="N/A" />
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>



<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="KJ.Modules.Monitoring.Views.TrendChartView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:KJ.Modules.Monitoring.ViewModels"
    mc:Ignorable="d">
    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="22" Text="趋势图" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <TextBox PlaceholderText="输入标签 Key" Width="250"
                     Text="{x:Bind ViewModel.SelectedTagKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="查询历史" Command="{x:Bind ViewModel.LoadCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="2" Text="{x:Bind ViewModel.StatusText}" Opacity="0.7" />

        <!-- Chart area -->
        <Grid Grid.Row="3" Height="300" Padding="0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Y-axis labels -->
            <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Stretch" Spacing="0" Margin="0,0,8,0">
                <TextBlock Text="{x:Bind ViewModel.MaxValue, Mode=OneWay}" FontSize="11"
                           VerticalAlignment="Top" HorizontalAlignment="Right" />
                <TextBlock Text="{x:Bind ViewModel.MinValue, Mode=OneWay}" FontSize="11"
                           VerticalAlignment="Bottom" HorizontalAlignment="Right" />
            </StackPanel>

            <!-- Canvas for the trend line -->
            <Border Grid.Row="0" Grid.Column="1" BorderBrush="Gray" BorderThickness="1" CornerRadius="4">
                <Canvas x:Name="ChartCanvas" Background="#08000000" />
            </Border>

            <!-- X-axis label -->
            <TextBlock Grid.Row="1" Grid.Column="1" FontSize="11" Opacity="0.6"
                       HorizontalAlignment="Center" Margin="0,4,0,0">
                <Run Text="数据点: " />
                <Run Text="{x:Bind ViewModel.PointCount, Mode=OneWay}" />
            </TextBlock>
        </Grid>

        <!-- Data table -->
        <ScrollViewer Grid.Row="4">
            <ItemsControl ItemsSource="{x:Bind ViewModel.Points}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:TrendPoint">
                        <StackPanel Orientation="Horizontal" Spacing="16" Padding="4,2">
                            <TextBlock Text="{x:Bind Timestamp}" Width="100" />
                            <TextBlock Text="{x:Bind Value}" FontWeight="SemiBold" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>

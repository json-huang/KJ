<?xml version="1.0" encoding="UTF-8" ?>
<ContentDialog
    x:Class="KJ.WinUI.Hosting.ExternalWindowPickerDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="嵌入外部窗口"
    PrimaryButtonText="嵌入"
    CloseButtonText="取消"
    DefaultButton="Primary"
    PrimaryButtonClick="OnPrimaryButtonClick">
    <Grid Width="680" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="420" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <TextBlock
                VerticalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="选择一个已运行的顶层窗口，将它嵌入当前 Dock 面板。" />
            <Button Click="OnRefreshClick" Content="刷新" />
        </StackPanel>

        <ListView
            x:Name="WindowList"
            Grid.Row="1"
            IsItemClickEnabled="True"
            ItemsSource="{x:Bind Windows, Mode=OneWay}"
            SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ExternalWindowInfo" xmlns:local="using:KJ.WinUI.Hosting">
                    <Grid Padding="8" RowSpacing="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <TextBlock FontWeight="SemiBold" Text="{x:Bind Title}" TextTrimming="CharacterEllipsis" />
                        <TextBlock
                            Grid.Row="1"
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{x:Bind DisplayName}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <TextBlock
            x:Name="EmptyHint"
            Grid.Row="2"
            FontSize="12"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="如果目标窗口没有出现，请确认它不是管理员权限窗口、最小化窗口或无标题窗口。" />
    </Grid>
</ContentDialog>
